{
  "name": "bachelor-monorepo",
  "private": true,
  "scripts": {
    "docker:up": "docker compose up -d --build",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs -f backend",
    "backend:build": "npm --workspace backend run build"
  },
  "workspaces": [
    "frontend",
    "backend",
    "dto"
  ]
}
